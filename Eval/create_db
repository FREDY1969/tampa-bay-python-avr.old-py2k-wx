#!/bin/bash

# create_db directory

set -e

usage() {
    echo "usage: create_db directory" >&2
    exit 2
}

[ $# -eq 1 ] || usage

database="$1/python-avr.db"

rm -f "$database"

sqlite3 "$database" < schema.ddl

sqlite3 "$database" < init.sql
